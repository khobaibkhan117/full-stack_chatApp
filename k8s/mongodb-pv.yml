apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pv
  namespace: chat-app
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  #persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /data
